export * from "./spriteStates";

export const PREFIX = "dkpp_";

export const ACTOR_STATES = ["Idle", "Walk", "Run", "Hurt", "Dead"];

export const CONFIG = {
	SPRITE: {
		BASE_SIZE: 128,
	},
	DYNAMIC_STEPS: true,
};
